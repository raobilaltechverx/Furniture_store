<!-- header -->
	<!-- //header -->
	<!-- banner -->
	<%= render :partial => "/home/banner" %>
	<!-- //banner -->
	<!-- banner-bottom -->
	<%= render :partial => "/home/belowbannerproducts" %>
	<!-- banner-bottom -->
	<!-- products -->
	<%= render :partial => "/home/products" %>
	<!-- // products -->
	<!-- products-bottom -->
	<%= render :partial => "/home/belowproductfeatures" %>
	<!-- //products-bottom -->
	<!-- sign-up -->
	<%= render :partial => "/home/subscriptionsignup" %>
	<!-- //sign-up -->
	<!-- footer -->
	<!-- //footer -->
  <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>        
